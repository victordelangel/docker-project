apiVersion: v1
kind: Service
metadata:
  name: {{ include "react-website.fullname" . }}
  labels:
    {{- include "react-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "react-website.selectorLabels" . | nindent 4 }}

apiVersion: networking.k8s.io/v1beta1
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: react-ingress
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
    - host: react-website.{{ .Values.dns.name }}
      http:
        paths:
          - backend:
              serviceName: react-service
              servicePort: http
            path: /